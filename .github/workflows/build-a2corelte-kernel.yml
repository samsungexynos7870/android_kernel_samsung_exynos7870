name: Build a2corelte kernel

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    container: ubuntu:20.04

    steps:
      - name: Checkout kernel
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          apt-get update
          DEBIAN_FRONTEND=noninteractive apt-get install -y \
            bc \
            bison \
            build-essential \
            ca-certificates \
            flex \
            git \
            libncurses5-dev \
            libssl-dev \
            python3

      - name: Install genuine Android GCC 4.9
        run: |
          rm -rf toolchain-4.9

          mkdir toolchain-4.9
          cd toolchain-4.9

          git init
          git remote add origin \
            https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9

          echo "=== FETCHING EXACT GCC 4.9 COMMIT ==="

          git fetch --depth=1 origin \
            a7bb6f39cd99b2fb26ecc2d3cb984d18cd259d24

          git checkout FETCH_HEAD

          echo "=== GCC FILE CHECK ==="
          pwd
          ls -lh bin/aarch64-linux-android-gcc
          file bin/aarch64-linux-android-gcc

          echo "=== GCC VERSION ==="
          bin/aarch64-linux-android-gcc --version

          if ! bin/aarch64-linux-android-gcc --version 2>&1 | grep -q "gcc version 4.9"; then
            echo "ERROR: Genuine GCC 4.9 was not obtained"
            exit 1
          fi

          echo "=== REAL GCC 4.9 VERIFIED ==="

      - name: Build kernel Image
        env:
          ARCH: arm64
          ANDROID_MAJOR_VERSION: p
        timeout-minutes: 180
        run: |
          export CROSS_COMPILE="$GITHUB_WORKSPACE/toolchain-4.9/bin/aarch64-linux-android-"

          echo "========================================"
          echo "       A2 CORE LTE KERNEL BUILD"
          echo "========================================"

          echo
          echo "=== COMPILER ==="
          "${CROSS_COMPILE}gcc" --version | head -n 1

          echo
          echo "=== DEFCONFIG ==="
          make exynos7870-a2corelte_defconfig

          echo
          echo "=== OLDDEFCONFIG ==="
          make olddefconfig

          echo
          echo "=== ENABLE SELINUX DISABLE SUPPORT ==="
          sed -i '/^# CONFIG_SECURITY_SELINUX_DISABLE is not set$/d' .config
          sed -i '/^CONFIG_SECURITY_SELINUX_DISABLE=/d' .config
          echo 'CONFIG_SECURITY_SELINUX_DISABLE=y' >> .config

          echo
          echo "=== CONFIG CHECK ==="
          grep '^CONFIG_SECURITY_SELINUX_DISABLE=' .config
          test "$(grep -c '^CONFIG_SECURITY_SELINUX_DISABLE=y$' .config)" = "1"

          echo
          echo "=== BUILDING IMAGE ==="
          echo "CPUs: $(nproc)"

          make -j$(nproc) Image 2>&1 | tee build.log

          echo
          echo "=== CHECKING IMAGE ==="

          test -s arch/arm64/boot/Image

          echo
          echo "========================================"
          echo "       IMAGE BUILD SUCCESSFUL"
          echo "========================================"

          ls -lh arch/arm64/boot/Image

          echo
          echo "=== FINAL IMAGE CONFIG ==="

          if [ -f scripts/extract-ikconfig ]; then
            chmod +x scripts/extract-ikconfig || true
            scripts/extract-ikconfig arch/arm64/boot/Image > final-image.config || true

            grep '^CONFIG_SECURITY_SELINUX_DISABLE=' final-image.config || true
          fi

          echo
          echo "=== DONE ==="

      - name: Upload kernel Image
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: a2corelte-kernel-Image-${{ github.run_number }}
          path: |
            arch/arm64/boot/Image
            build.log
            final-image.config
          if-no-files-found: warn
